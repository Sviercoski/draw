function setup() {
  createCanvas(600, 600);
background("red");}

function draw() {
  
  
  stroke('blue')
  fill("blue")
  
 //console.log (mouseIsPressed);
  
  if(mouseIsPressed) {square(mouseX, mouseY, 20,20)};
  
 
  
}
